- name: Uninstall software packages
  become: yes
  community.general.aptitude:
    name: "{{ item }}"
    state: latest
  loop: "{{ software_uninstall_list }}"
  tags:
    - software
    - uninstall
    - aptitude
